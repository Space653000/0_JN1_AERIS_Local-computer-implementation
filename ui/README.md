# AERIS UI

Portable-company UI has three canonical surfaces: Dashboard (company state), Workspace (Human command surface), Services (runtime/trust/health console). UI status is a projection of backend evidence, never an independent truth source. The visual North Star remains the read-only core UI baseline.