# Professional Company Build — execution record

Authority: Human request, 2026-09-03. Core remains read-only. No GitHub push,
Claude execution, paid tools, or user Home configuration changes.

## Acceptance

100 canonical roles must each execute a domain-relevant deterministic local
capability. A shared persona, prompt or successful LLM call is insufficient.
L0 registry; L1 contract; L2 executed local capability; L3 evaluated bounded
domain baseline; L4 requires real instrument/calibration/expert evidence.
Synthetic evaluation never upgrades to L4. Maturity is derived from executable
contracts and hash-verified evaluation records, not manually assigned green labels.

## Batches

1. Numerical toolchain, factory schemas and deterministic skill implementations.
2. Analytical Golden/negative/regression suites; Speaker and Microphone methods.
3. 100 role packs with explicit role-to-skill mappings and 24 product profiles.
4. Capability-based routing, task/workflow execution, evidence and adversarial review.
5. Provenance corpus and append-only project/experiment/decision/lesson memory.
6. Capability matrix, all-role evaluation, regression tests and local commit.

Baseline: canonical groups agree with the Core `aeris-data.js`; 100 registry seats,
5 registered existing Skills, 3 Method files, 189 existing unit tests passing.
No per-seat factory execution/evaluation evidence exists at the start of this pass.

Numerical scope must declare assumptions, SI units, applicable data conditions,
counter-hypotheses and uncertainty. Reject malformed/nonfinite or out-of-scope
inputs. Licensed CAE/measurement interfaces remain optional contracts.

Evidence is separate from Memory. Automated reviewers supply reproducible
counterchecks and disagreement records, never substitute for Human G2/G4/G5 approval.

Infrastructure changes are limited to integration needed by this capability build.

## Implemented scope and truthful counts

- 100 canonical packs: Chief Council 8; Speaker 18; Microphone 18;
  Product Chiefs 24; Experts 20; Operations 12. All have domain-mapped executables.
- 42 distinct new Skills and Methods; 42 analytical Golden cases, 42 negative
  cases and 42 repeated-output regression cases. The pre-existing 5 Skills are
  not included in these factory counts. One fixture per method is a baseline,
  not comprehensive industrial qualification.
- All 42 method workflows exercised SQLite, task state, plots, report, sealed
  Evidence and exact replay; all 24 product-specific plans exercised their own
  Product Chief Pod, workflow and Evidence.
- Role maturity is recalculated using sealed evidence, source fingerprints,
  artifact fingerprints and complete contracts. Altering a label cannot grant L4.
- Local AI intake was run with loopback qwen3:4b-instruct. It proposes a known
  method and required input fields; it does not invent measurements or execute
  unapproved numerical work.
- Durable memory has 14 typed event channels, a verified SQLite hash chain,
  source-linked deterministic retrospectives and bounded hot context. It is not
  administrator-proof WORM, and its summaries are not independent evidence.

## Use locally

Open `/workspace?theme=dark` or `/workspace?theme=light`, then scroll to
「聲學工程能力矩陣 / 執行專業工程能力」. Choose a seat and Skill, load an explicitly
synthetic Golden case or supply schema-valid data, then execute. The returned
task/workflow/Evidence IDs refer to persisted local objects, not mock responses.
The optional AI button understands the engineering objective and lists missing
input fields; it does not silently choose synthetic data.

CLI from `C:\0_JN1_AERIS`:

```powershell
.\.venv\Scripts\python.exe -B -m aeris_runtime.engineering.factory status
.\.venv\Scripts\python.exe -B -m aeris_runtime.engineering.factory evaluate-all
.\.venv\Scripts\python.exe -B scripts/accept-professional-company.py
```

Actual records: `.aeris/capability-factory/CAPABILITY_MATRIX.json`,
`PROFESSIONAL_ACCEPTANCE.json`, `LOCAL_AI_INTAKE.json`, `harness.sqlite3`,
`reports/`, and `.aeris/evidence/` sealed run bundles.

## Remaining authority and external boundaries

L4 needs genuine DUT/instrument/fixture/calibration and expert acceptance.
Licensed CAE and professional measurement adapters remain optional contracts;
free lumped/analytical methods do not implement full nonlinear FEM/BEM or
professional equipment equivalence. Standards contain metadata and authored
notes, not unlicensed copyrighted full text. Clinical, production, patent/FTO,
formal release and R3/R4 authority remain Human gates. Real reboot/logoff and
second-device relocation are outside this software capability acceptance.

No GitHub push or CI run was performed for this local-only change. Previous
remote CI evidence is not evidence for this new local commit.
