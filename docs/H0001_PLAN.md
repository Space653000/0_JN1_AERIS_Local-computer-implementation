# H0001 — depth correction plan (P02 NOT ACCEPTED)

Fixed point: 06713e1 (construction 0752121). PR #32 remains immutable history.
User's H0001 specification is controlling authority. No Core changes, no paid
tools, no Claude, no private data publication, no main push/merge.

1. Correct maturity first: Skill execution/Golden evidence cannot yield L3.
   Require valid Method, negative behavior and profession relevance for L2;
   require separately sealed role-domain-suite evidence for L3. Preserve old
   records and record downgrade causes. Do not target a quota of L3 seats.
2. Portable identity: use existing tracked registry, pinned semantic digest
   tied to core.lock SHA and a reviewed Core source hash. If cache exists,
   additionally compare it. Clean checkout must not need .aeris. Tampering
   either registry or pin must fail independently of the runtime cache.
3. PPTX: separate schema/upstream/hash/unsigned policy contract validation from
   local byte verification. Missing local artifacts is NOT_PRESENT, never
   VERIFIED; malformed contract and existing tampered bytes remain failures.
4. Author profession-specific profiles for all 100 seats, including explicit
   neighboring-role distinctions and justified standards (possibly none).
   Product profiles contain topology/use/lifecycle/manufacturing decisions.
5. Separate Role Acceptance Factory from Skill Factory. Share low-level signal
   builders but author role-specific questions/expected decisions. Every L3
   suite must include multiple scenarios, positive/boundary/negative,
   counterhypothesis, integrity and qualified independent reviewer checks.
   Prioritize all 36 acoustic seats and 24 product chiefs; other seats must
   remain honestly L1/L2 if their professional suite is not sufficient.
   No generic wrapper / renamed fixture / counting scenarios as depth.
6. Add free standards lifecycle/applicability/change-impact metadata engine;
   product-specific and speaker/microphone multi-method decisions. A case must
   establish a discriminating engineering decision, not merely finite numbers.
7. Dynamic reviewer selection uses skill/domain/product/transducer/lifecycle/
   risk/evidence/maturity/independence constraints. No default universal R098.
   Missing qualified review blocks acceptance; automation never grants Human L4.
8. Classify all actual knowledge sources with provenance, rights, hash,
   retrieval/creation distinction and role mappings; count public external,
   authored, synthetic and generated separately. Do not fabricate retrievals.
9. Preserve append-only memory but replace canned distillation with event-
   derived clusters, disagreements, summaries and next discriminating tests.
   Empty source means no insight, not invented lessons. Memory != Evidence.
10. Eight cross-company challenges execute the real task/workflow/Evidence/
    review/revision/report/memory/replay path. Include deliberately rejected
    and revised hypotheses; no fixed PASS stage records.
11. Live UI shows independent contract, executable, role acceptance, skill,
    method, source-class and real Evidence coverage. Recalculate from evidence.
12. Clean tracked export without .aeris: local Windows full tests and browser
    tests; new Draft PR Windows/Ubuntu CI must pass before handoff. Do not use
    local caches to hide CI failures. Keep .aeris artifacts private and publish
    only sanitized hash indices. New H0001 branch based on new construction SHA,
    never update PR #32; no merge.

Gates: independently review this plan before implementation; focused tests per
batch and permanent tamper/negative regressions; full clean tests at integration;
read-only Standards + Spec review of final diff. Review objections are recorded
here. Three consecutive true external blockers stop only the blocked batch;
licenses/physical authority/unsafe actions are never inferred. Reversible local
commits preserve rollback; no raw Evidence mutation. Scope expansion beyond the
explicit H0001 work requires Human direction, not silent additional products.

## Independent plan review disposition

Isolated same-model read-only review; not Human Approval. All six findings
accepted before implementation:

- Decision oracles are authored separately from planners/profiles. Mutation
  tests must reject wrong topology, units, risk and swapped neighboring seats.
- L2 requires a nonempty role -> professional decision -> Method -> Skill
  contract and corresponding executable input/output, negative and boundary
  evidence. Arbitrary mapped Skills or empty checks cannot grant L2.
- Reviewer bootstrap uses separately evidenced bounded review capability, not
  the L3 being awarded. All entrypoints share eligibility; runtime overrides
  are forbidden. Missing review preserves runs as REVIEW_BLOCKED. Different
  software role IDs are not external professional independence.
- Sealed records, not index metadata, bind role ID, contract, Skill source,
  artifacts, suite, acceptance engine and review policy hashes. Swap/stale/index
  regressions must remove eligibility. Index is only a locator.
- Offline Core pin verifies consistency, not independent authenticity against
  coordinated registry+pin edits. CI additionally verifies the locked upstream
  source. Pin includes Core commit, source SHA-256, extraction version and
  order-sensitive semantic role digest.
- Challenges prove revised inputs change decisions after concrete rejection;
  unrevised contradictions block acceptance. Memory is reconstructible from
  original events, never recursive distillation of itself or canned insight.

Approved public test seams from H0001: capability matrix/evaluation, canonical
role registry, PPTX provenance verification, Role Router, role-domain acceptance,
Memory Harness, workflow challenges and live HTTP truth. TDD is applied at
these seams; no extra product authority is inferred.
