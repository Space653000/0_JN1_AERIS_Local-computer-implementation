# AERIS LOCAL SOFTWARE COMPLETION PASS

> Purpose: close every safe, zero-cost, software-only local gap before declaring the local platform baseline complete.
>
> Execution target: **Codex only in `C:\0_JN1_AERIS`**.

## Authority and boundaries

1. `https://github.com/Space653000/0_JN1_AERIS` is WHAT / Canonical Core / SSOT. It is read-only for this workflow. Never modify or push Core.
2. `0_JN1_AERIS_Local-computer-implementation` is HOW.
3. Do not use Claude Code or require a Claude token.
4. Do not purchase or install paid professional software.
5. Do not push GitHub from the local execution pass.
6. All files, clones, caches, builds, tests and artifacts must remain under `C:\0_JN1_AERIS`.
7. Do not stop because `config/maturity.json` contains `NOT_IMPLEMENTED`; any capability that is safe, zero-cost and software-local must be implemented.
8. Only genuine license, external equipment, physical measurement/calibration, Human Approval, or actual reboot/logoff evidence may remain as Human/External Gates.

## A. Core UI must converge to the local production UI

Use the local read-only Core cache copies of:

- `index.html`
- `workspace.html`
- `services.html`
- `aeris.css`
- `aeris-theme.js`
- `aeris-data.js`

as the local production UI **visual + information architecture SSOT**.

Do not copy static fake state. Preserve Core layout, hierarchy, sidebars, cards, information architecture and Traditional Chinese design, while binding every available value to real Local Implementation APIs.

Required six views:

- Dashboard Dark
- Workspace Dark
- Services Dark
- Dashboard Light
- Workspace Light
- Services Light

Requirements:

- Light/dark differ only by theme; functionality and information architecture are identical.
- Dashboard / Workspace / Services must converge to Core UI and must not maintain a second simplified product UI.
- Runtime state must come from real backend/API evidence. Never hard-code `HEALTHY`, `VERIFIED`, `100%`, or `READY`.
- Preserve `Dashboard != Truth`.
- Refresh mutable state every 10 seconds.
- Refresh immediately on `window.focus` and `visibilitychange` return.
- Reload browser assets after Codex changes the active frontend asset set.
- Keep all six pages HTTP 200 throughout the build whenever a service cutover is not technically unavoidable; any cutover failure must be preserved as evidence.

## B. Make Core pages operational

### Dashboard

Bind real local truth for at least:

- company opening
- runtime/model
- 100 roles
- skills
- standards
- knowledge
- workflow runs
- projects/tasks
- evidence
- verification
- expected-runs
- audit
- maturity
- health
- machine/GPU
- watchdog
- offline state

### Workspace

Operationalize Core fields:

- Product
- Speaker / Microphone / Both
- Lifecycle
- Risk R0-R4
- Evidence Tier
- Standards strategy
- Engineering Objective
- Dynamic Pod
- Requirement
- Hypothesis
- Evidence Needed
- canonical workflow timeline

`Build Temporary Pod` must call the real Role Router, not a hard-coded Core prototype role array.

Creating a task must persist into the real SQLite/task/workflow system.

### Services

Bind the Core five-plane architecture to real runtime telemetry:

- CONTROL
- KNOWLEDGE
- EXECUTION
- TRUST
- OPERATIONS

Each service must expose at least:

- state
- reason
- evidence/ref
- last update
- capability maturity

A live process alone must never imply `HEALTHY`.

## C. Classify and close maturity gaps

Inspect at minimum:

- `config/maturity.json`
- `docs/DEFINITION_OF_COMPANY_DONE.md`
- Core requirements
- API
- UI
- workflows
- roles
- skills
- methods
- standards
- evidence
- verification
- reproduction
- health
- installer
- watchdog
- scheduler
- offline
- portability
- tests

Classify every gap as exactly one of:

1. `SOFTWARE_LOCAL_FIXABLE`
2. `HUMAN_GATE`
3. `EXTERNAL_LICENSE`
4. `PHYSICAL_HARDWARE`
5. `REBOOT_LOGOFF_REQUIRED`

For every `SOFTWARE_LOCAL_FIXABLE` gap:

`implement -> test -> negative/fail-closed test -> integrate -> update truth/maturity -> produce evidence`

Do not leave a software-local gap at `NOT_IMPLEMENTED`.

## D. Known local issues that must be resolved or truthfully gated

At minimum:

- Autopilot result must explicitly include:
  - `unresolved_software_gaps`
  - `remaining_external_blockers`
  - `remote_write_performed`
  - `local_only_scope`
- Analyze and correct the Scheduled Task `LastTaskResult` path for everything fixable in the current session.
- Complete software persistence contract / watchdog / supervisor evidence.
- Integrate the locally usable PPTX Beautify Lock capability into the AERIS Skill Registry without claiming PPTX production acceptance unless a real input PPTX exists.
- Every Skill executable/source used in this path must carry SHA-256 provenance.
- Never call an unsigned EXE a trusted signed binary.
- Never modify the user's Home `.codex` or `.claude` directories.
- Keep review sources and build artifacts inside `.aeris` under the target workspace.

## E. Free local baseline adapters

Do not stop merely because COMSOL, MATLAB, APx, KLIPPEL, SoundCheck or ACQUA are unavailable.

Represent the distinction as:

`PROFESSIONAL_TOOL_UNAVAILABLE -> FREE_LOCAL_BASELINE`

Use Python / NumPy / SciPy / soundfile / librosa or other already-available legal zero-cost local tools for capabilities such as:

- FFT
- FR
- THD
- SNR
- SPL/basic calibration model
- impulse response
- octave / fractional-octave
- filtering
- transfer function
- STFT
- coherence
- basic speaker/mic analysis
- deterministic plotting
- report/evidence generation

UI and evidence must clearly distinguish:

`FREE_BASELINE != LICENSED_PROFESSIONAL_VERIFIED`

## F. Acceptance

After implementation, run from the beginning:

- full unit tests
- Windows-specific tests
- browser semantic E2E
- Dashboard/Workspace/Services x light/dark
- screenshot baseline
- accessibility baseline
- API contract tests
- negative / fail-closed tests
- offline inference
- workflow execution to `EVIDENCED`
- Claim Guard
- Core integrity
- evidence integrity
- audit chain
- watchdog recovery
- scheduler
- six-page continuous monitoring
- Autopilot
- Local Acceptance

Then rescan every maturity gap.

## Stop condition

Tests being green is not sufficient.

The only software stop condition is:

`SOFTWARE_LOCAL_FIXABLE = 0`

The only remaining items may be:

- genuine Human Gate
- genuine licensed/paid external software
- genuine physical equipment/calibration
- evidence that specifically requires reboot/logoff

## Required final report

Report only:

1. Software-local gaps before / after
2. What was completed
3. Six page URLs
4. Tests
5. Local commit
6. Remaining Human/External gates
7. Whether `SOFTWARE_LOCAL_FIXABLE = 0` is true

If it is not zero, continue. Do not end early.
