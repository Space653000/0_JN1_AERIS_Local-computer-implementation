# Firmware Integration

Firmware binaries are product/hardware-specific. This repo stores firmware interface contracts, version manifests, safe update/rollback procedures and test adapters; proprietary firmware images or credentials must not be committed unless redistribution is explicitly allowed.