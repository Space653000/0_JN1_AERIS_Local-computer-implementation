# Evidence

Evidence = what physically/computationally happened in a run. It is separate from Memory.

Canonical bundle includes run manifest, requirement/method snapshots, input hashes, raw/processed results, execution log, environment/tool versions, standards, validation, reviewer and approval.