---
name: failure-hypothesis-experiment-baseline
description: Evaluate explicit failure hypotheses and rank controlled local discriminating experiment models.
---

Read the Method and exact input schema. Require distinct product and test-system
mechanisms, normalized priors, complete likelihoods, unique observation/source IDs
and explicit unverified conditional-independence assumptions. Reject missing or
inconsistent rows; never treat supplied likelihoods as physical observations.

Use log-space inference and high-precision expected conditional entropy. Rank informative available R0/R1 experiment models; suggest only, do not operate hardware.
Preserve root-cause, recurrence and calibration gates even when posterior policy
passes. Qualification requires this seat's own suite. No shared Skill result can
award role-wide L3 or Human authority. Seal inputs, source, Method and outputs;
Memory never substitutes for Evidence. Challenge revisions must append exactly
one hypothetical outcome of the first sealed selected experiment without changing
old observations, hypotheses, policy or experiment cost/availability.

