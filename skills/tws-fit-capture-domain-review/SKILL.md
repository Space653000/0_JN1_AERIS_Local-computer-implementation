---
name: tws-fit-capture-domain-review
description: Review seal loss and distinguish wind exposure from stationary capture noise.
---

# tws-fit-capture bounded review

1. Read the Method referenced by manifest.json and the executor input schema.
   Supply explicit declared-unit parameters, candidate assertions and applicability.
2. Run this Skill through aeris_runtime.skills_runtime.run_skill. A malformed
   candidate, wrong units or out-of-scope context rejects the request.
3. Report each disagreement and discriminating experiment. BOUNDED_REVIEW_ACCEPT
   means scoped report consistency, including a correctly rejected design.
4. Use the separate role suite and intact sealed evidence before selecting this
   seat for review. Retain the executor evidence and qualification references.

The reviewer independently checks review seal loss and distinguish wind exposure from stationary capture noise.
It never calls the executor to generate its oracle. Synthetic qualification is
bounded software execution, not qualified Human credentials, physical truth,
whole-role L3, instrument calibration, licensed-tool verification or release.
