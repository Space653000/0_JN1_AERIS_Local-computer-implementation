{
  "type": "object",
  "required": ["skill_id", "values", "input_sha256", "implementation_sha256", "physical_measurement_verified"],
  "properties": {
    "values": {"type": "object", "required": ["proposed_correction_db", "boost_upper_db", "cut_upper_db", "required_headroom_db", "checks", "disposition"]},
    "physical_measurement_verified": {"const": false}
  }
}
