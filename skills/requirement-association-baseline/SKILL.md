---
name: requirement-association-baseline
description: Evaluate versioned requirement/test/result associations and supplied uncertainty intervals without claiming physical acceptance.
---

Read the Method and exact input schema. Fix required test IDs/revisions before
reading actual links; missing associations stay in both coverage denominators.
Match requirement, test and result measurand/reference/units and configuration.
Never convert implicitly or interpret a content hash as source authenticity.

Compare exact rational supplied intervals to limits; retain exact endpoints even when approximate display values coincide.
Reject duplicates, dangling links and tampered payloads. Keep stale revisions,
failed results and uncertainty-crossed limits visible. Only synthetic or supplied
unverified records are in scope. Real Evidence count remains zero; no customer,
physical or Human acceptance. Seal source/input/output for the actual workflow.
Role qualification requires its own suite, not a shared traceability Skill PASS.
